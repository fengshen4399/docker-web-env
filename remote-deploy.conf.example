# 远程部署配置文件示例
# 复制此文件为 remote-deploy.conf 并修改相应配置

# 服务器配置
SERVER_IP=192.168.1.100
SERVER_USER=root

# 认证方式 (key 或 password)
AUTH_TYPE=key

# SSH 密钥路径 (当 AUTH_TYPE=key 时使用)
SSH_KEY_PATH=~/.ssh/id_rsa

# SSH 密码 (当 AUTH_TYPE=password 时使用)
SSH_PASSWORD=

# 部署包类型 (full, minimal, configs)
PACKAGE_TYPE=minimal

# 目标目录
TARGET_DIR=/home/docker-web-env

# 部署后是否自动启动服务
AUTO_START=true

# 部署后是否显示访问地址
SHOW_ACCESS_INFO=true
